# 0.7.5 - 2026-06-08

## Changed

- `simulator/js/robot-scene.js` 已从普通 joint-pivot 模型升级为 servo-actuator model。
- 每个舵机现在都包含 `servoCase`、`servoHorn`、`childRoot`，下一级结构挂在舵盘输出上。
- `setPose` 现在直接设置各 ID 舵机的 `hornGroup.rotation`，而不是把抽象关节球当旋转中心。
- 腿部与手臂链路都已按真实舵机串联关系重建。
- `knowledge/docs/14-servo-layout.md` 已同步说明当前 3D 预览器采用舵机驱动的分层单轴 FK。

## Verified

- 对 `simulator/js/config.js`、`i18n.js`、`rob-parser.js`、`robot-scene.js`、`simulator-app.js`、`main.js` 建立临时 `.mjs` 镜像后执行 Node `--check`，结果 `syntax_check=ok`。
