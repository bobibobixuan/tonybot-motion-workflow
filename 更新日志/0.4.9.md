# 0.4.9 - 2026-05-22

## Changed

- 将 `169号工业校歌队列广播体操合并版` 压缩为 `1分16秒` 精简版，主体保留到 `beat-30-torch-passing`，删除后半段重复展开动作。
- 收尾改成短收势后直接 `19号快速立正` 回正，整体更紧凑，同时保持动作语义连贯。
- 重新生成 `169` 的 `.rob`、报告和时间线，新版结果为 `193` 帧、`76000ms`、`violations=0`。

## Verified

- `C:\\Users\\21996\\.cache\\codex-runtimes\\codex-primary-runtime\\dependencies\\python\\python.exe python-toolkit/build_industrial_school_song_queue_safe_merged.py`
