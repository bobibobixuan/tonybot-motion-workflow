# 0.1.5 - 2026-05-12

## Added

- `rob_compose.py` 新增顶层 `guards` 配置，支持用 `pre_segments` / `post_segments` 在编舞主体前后插入固定保护段。

## Changed

- 将“完整展示类舞蹈开场前后必须回正”正式写入 `动作安全规范.md` 和 `编舞工作流说明.md`。
- `编舞/162号科目三完整版.json` 改为通过 `guards` 显式注入“302号快速回正 -> 0号立正”的前后保护段，避免上机时受上一段残余姿态影响。

## Verified

- `uv run python -m py_compile rob_reverse.py rob_crypto.py rob_compose.py rob_safety.py dance_workflow.py main.py`
- `uv run python dance_workflow.py build "编舞\\162号科目三完整版.json"`
- `uv run python rob_safety.py "动作\\162号科目三完整版.rob"`
