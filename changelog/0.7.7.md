# 0.7.7 - 2026-06-14

## Changed

- 将网页模拟器重构为“三栏校对结构”：左栏映射总览、中栏 3D 视口、右栏 16 路滑块与时间轴。
- `simulator/js/config.js` 重写为 `SERVO_LAYOUT` 驱动，不再散落维护多份 `neutral` / `joint` / `label` 硬编码。
- `data/servo-map.json` 补齐 `neutral`、`direction_sign`、`tested_change`、`tested_motion_zh`、`group`、`mirror_id`，使其可直接用于页面和文档。
- `simulator/js/robot-scene.js` 改为从 `SERVO_LAYOUT` 派生渲染元数据，避免页面层和渲染层出现两套定义。
- 重写 `simulator/README.md`、`knowledge/docs/13-3D动作预览器规范.md`、`knowledge/docs/14-servo-layout.md`，统一为最新关节映射表口径。
- 更新根 `README.md` 的打开方式与版本信息。

## Verified

- `node --check` 检查模拟器 JS 模块语法。
- `ConvertFrom-Json` 检查映射 JSON 和 i18n JSON。
- `python -m py_compile tools/python/tonybot_physics.py`
- 实际打开本地模拟器页面并检查 DOM 与控制台状态。
