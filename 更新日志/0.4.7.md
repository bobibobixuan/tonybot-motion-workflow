# 0.4.7 - 2026-05-18

## Fixed

- 将 `python-toolkit/编舞/167号工业校歌队列广播体操版A.json` 的 `beat-07-reform-spring` 从 `389号手写低位提振` 替换为 `391号手写开场点头`，修正真机在约 `17s~18s` 的危险动作。
- 将 `python-toolkit/编舞/169号工业校歌队列广播体操合并版.json` 同步替换为 `391号手写开场点头`，避免合并版继续继承该失稳段。

## Changed

- 更新 `python-toolkit/文档/10-队列安全动作分组.md`，将 `389号手写低位提振` 从队列推荐集合移出，并补充该动作的真机风险说明。
