# 0.4.0 - 2026-05-17

## Changed

- 将现有 Python 工具链整合到 `python-toolkit/` 目录，为新的 Python 架构腾出根目录空间。
- 所有源码（`rob_*.py`、`dance_workflow.py`、`main.py`、`bip_industrial_school_song.py`、`rob_decrypt_with_upper.ps1`）迁移到 `python-toolkit/`。
- 动作库（`动作/`）、编舞（`编舞/`）、动作库解密（`动作库解密/`）、动作库解析（`动作库解析/`）、动作模块（`动作模块/`）迁移到 `python-toolkit/`。
- 文档（`文档/`）和更新日志（`更新日志/`）迁移到 `python-toolkit/`。
- 更新 `README.md`、`AGENTS.md`、`CHANGELOG.md`、`VERSION` 以反映新的目录结构。
