# 0.7.3 - 2026-06-08

## Changed

- `simulator/index.html` 现通过 `./js/main.js` 作为新入口启动，模拟器逻辑拆分为可维护的独立模块。
- 3D 预览器已采用分层单轴 FK：右腿 `ID1 -> ID5 -> ID4 -> ID3 -> ID2`，左腿 `ID9 -> ID13 -> ID12 -> ID11 -> ID10`；右臂 `ID8 -> ID7 -> ID6`，左臂 `ID16 -> ID15 -> ID14`。
- 膝盖与肘部在渲染层中为严格单轴 hinge。
- `ID2/ID10` 与 `ID7/ID15` 仍保留为待实测轴，但已改为临时单轴近似显示，不再按万向关节处理。
- `knowledge/docs/14-servo-layout.md` 已同步改写为“已采用分层单轴 FK，未确认轴仍为临时单轴近似”。

## Verified

- 对 `simulator/js/config.js`、`i18n.js`、`rob-parser.js`、`robot-scene.js`、`simulator-app.js`、`main.js` 建立临时 `.mjs` 镜像后执行 Node `--check`，结果 `syntax_check=ok`。
